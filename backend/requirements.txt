# API runtime deps ONLY (kept light so it fits Vercel serverless).
# The ML/engine extras (pandas, scikit-learn, ...) live in requirements-engine.txt
# and are only needed by the CLI / daily GitHub Action — never by the web API.
fastapi>=0.115
uvicorn[standard]>=0.30
sqlalchemy>=2.0
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0
PyJWT>=2.8
bcrypt>=4.1
python-multipart>=0.0.9
email-validator>=2.1
requests>=2.31
psycopg[binary]>=3.1
